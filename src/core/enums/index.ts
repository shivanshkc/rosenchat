/**
 * @description OAuthProvider provides type-safety with OAuth provider names.
 */
export enum OAuthProvider {
  Google = 'GOOGLE',
}

/**
 * @description HomeComponentLayoutMode provides type-safety and re-usability
 * with Home page layout modes.
 */
export enum HomeComponentLayoutMode {
  BothOpen = 'BOTH_OPEN',
  ChatListOpen = 'CHAT_LIST_OPEN',
  ChatBoxOpen = 'CHAT_BOX_OPEN',
}
